.charts-section {
    display: flex;          
    flex-wrap: wrap;    
    gap: 20px;          
}

.chart-container {
    flex: 1; 
    min-width: 300px; 
    max-width: 70%; 
    transition: transform 0.3s ease; 
}

.chart-container:hover {
    transform: scale(1.2);
    z-index: 20;
}

.fullscreen-chart {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw; 
    height: 100vh; 
    background: rgba(0, 0, 0, 0.8); 
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000; 
    overflow: hidden; 
    transition: opacity 0.3s ease;
}

.fullscreen-chart .chart-container {
    width: 70vw; 
    height: auto; 
    background: white; 
    border-radius: 8px; 
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); 
    overflow: hidden; 
    position: relative;
    box-sizing: border-box; 
}

body.fullscreen-active {
    overflow: hidden; 
}

.fullscreen-chart .chart-container:hover {
    transform: none; 
    cursor: default;
}